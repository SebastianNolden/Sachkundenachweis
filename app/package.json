{
  "name": "sachkundenachweis-mobile-app",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor/Android-App fuer den Sachkundenachweis-Lerntrainer (Meilenstein 1: content.json laden, eine Frage anzeigen).",
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "build": "npm run sync-content",
    "cap:add:android": "cap add android",
    "cap:sync": "npm run build && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^8.1.1",
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
